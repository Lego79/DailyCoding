package ProgrammersLv0;

import java.util.Arrays;
import java.util.stream.Collectors;
import java.util.stream.Stream;

public class DeleteAEIOU {

    class Solution {
        public String solution(String my_string) {
            String answer = "";

//            String[] str = new String[5];



            String[] str = {"a", "e", "o", "u", "i"};

            Strin



            return Arrays.stream(my_string).map(s -> Arrays.stream(str))

            Stream<String> =
            list.s

            return (int) Arrays.stream(s1).map(s -> Arrays.stream(s2).collect(Collectors.toList()).contains(s)).filter(a -> a).count();


            return answer;
        }
    }

}
